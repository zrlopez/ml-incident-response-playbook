# src/schemas — Pydantic response/request models for the API layer.
# All external-facing shapes are defined here so the API layer
# validates both inputs (request bodies) and outputs (response bodies).
