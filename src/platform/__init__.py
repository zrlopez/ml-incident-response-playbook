# src/platform — Infrastructure-level concerns.
# Engine construction, session factories, connection pooling, and other
# platform primitives that are independent of business domain logic.
# Route handlers should use these via FastAPI Depends() injection.
