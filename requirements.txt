# ──────────────────────────────────────────────────────────────────────────────
# requirements.txt — production dependencies
#
# IMPORTANT: This file lists ALL project dependencies for completeness.
# The API container (infrastructure/Dockerfile.hardened) installs only a
# filtered subset — Airflow and dbt are excluded from the API image.
# See Dockerfile.hardened comments for the exact install command.
#
# Last reviewed: 2026-05-23
# ────────────────────────────────────────────────────────────────────────────

# ── Web framework ──────────────────────────────────────────────────────────────────
fastapi==0.115.12
uvicorn[standard]==0.34.2
python-multipart==0.0.20

# ── Authentication ────────────────────────────────────────────────────────────────
PyJWT[crypto]==2.10.1
# passlib REMOVED: last release 2022, bcrypt>=4.0 ABI incompatibility under passlib 1.7.4.
# Password hashing is now handled by direct bcrypt calls in api/app.py.
bcrypt==4.2.1
cryptography==44.0.3

# ── Rate limiting ────────────────────────────────────────────────────────────────
slowapi==0.1.9

# ── Data validation ───────────────────────────────────────────────────────────────
pydantic==2.11.4
pydantic-settings==2.9.1

# ── Structured logging ─────────────────────────────────────────────────────────────
structlog==25.3.0

# ── Database (async-ready) ─────────────────────────────────────────────────────────
# Production: postgresql+asyncpg
# Local / test: sqlite+aiosqlite (configured via DATABASE_URL env var)
asyncpg==0.30.0
aiosqlite==0.21.0
sqlalchemy[asyncio]==2.0.41
alembic==1.16.1

# ── Redis (distributed JWT denylist) ──────────────────────────────────────────────
# hiredis provides C-extension parser for ~10x read throughput on token checks.
redis[hiredis]==5.2.1

# ── HTTP client (internal service calls, test transport) ───────────────────────
httpx==0.28.1

# ── Observability ──────────────────────────────────────────────────────────────────
opentelemetry-api==1.33.1
opentelemetry-sdk==1.33.1
opentelemetry-instrumentation-fastapi==0.54b1
opentelemetry-exporter-otlp-proto-grpc==1.33.1
prometheus-fastapi-instrumentator==7.1.0

# ── ML / Data ────────────────────────────────────────────────────────────────────
numpy==2.2.6
pandas==2.2.3
scikit-learn==1.6.1

# ── Orchestration / Airflow ─────────────────────────────────────────────────────
# NOT installed in the API container. Deploy Airflow separately and sync DAGs
# via git-sync. See infrastructure/Dockerfile.hardened for the exclusion.
apache-airflow==2.11.0

# ── AWS SDK ──────────────────────────────────────────────────────────────────────
boto3==1.38.22

# ── Utilities ──────────────────────────────────────────────────────────────────────
python-dotenv==1.1.0
tenacity==9.1.2

# ── dbt ───────────────────────────────────────────────────────────────────────────
# NOT installed in the API container. Run dbt in a dedicated Airflow worker image.
dbt-core==1.9.4
dbt-postgres==1.9.3
