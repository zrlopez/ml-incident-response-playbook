# =============================================================================
# ML Incident Response API — Dependencies
# =============================================================================
# Remediation: 2026-05-23
#
# SECURITY CHANGES:
#   - python-jose REMOVED (CVE-2024-33663 alg confusion, unmaintained)
#     REPLACED WITH: PyJWT[crypto]==2.9.0
#   - passlib REMOVED (unmaintained since 2020, no security patches)
#     REPLACED WITH: bcrypt==4.2.1 directly
#   - numpy upgraded 1.26.4 -> 2.1.3 (1.x reaching EOL)
#   - slowapi added for rate limiting on /auth/token
#
# LOCKFILE: Generate with:
#   pip-compile requirements.in --generate-hashes --output-file requirements.txt
# Then install with:
#   pip install --no-cache-dir --require-hashes -r requirements.txt
# =============================================================================

# ─── Web framework ────────────────────────────────────────────────────────────
fastapi==0.115.6
uvicorn[standard]==0.32.1
httpx==0.28.1

# ─── Authentication & Security ──────────────────────────────────────────────────
PyJWT[crypto]==2.9.0   # Replaces python-jose (CVE-2024-33663)
bcrypt==4.2.1           # Replaces passlib (unmaintained since 2020)
slowapi==0.1.9          # Rate limiting (was stub; now implemented)
cryptography==43.0.3    # CVE-2023-49083 patched in 41.0.6+

# ─── Validation & serialization ─────────────────────────────────────────────────
pydantic==2.10.4
pydantic-settings==2.7.0

# ─── Observability & logging ───────────────────────────────────────────────────
structlog==24.4.0

# ─── Data & ML ───────────────────────────────────────────────────────────────────
numpy==2.1.3            # Upgraded from 1.26.4 (1.x EOL approaching)
pandas==2.2.3
scikit-learn==1.5.2

# ─── Workflow orchestration ──────────────────────────────────────────────────────
apache-airflow==2.10.4  # Upgraded from 2.9.1 (latest patched)

# ─── Cloud & storage ──────────────────────────────────────────────────────────
boto3==1.35.74

# ─── HTTP & networking ─────────────────────────────────────────────────────────
requests==2.32.3        # CVE-2023-32681 patched
jinja2==3.1.5           # CVE-2024-22195 patched in 3.1.3+
MarkupSafe==3.0.2

# ─── Testing (dev/CI only) ────────────────────────────────────────────────────
pytest==8.3.4
pytest-asyncio==0.25.0
pytest-cov==6.0.0
