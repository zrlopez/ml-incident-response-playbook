# ============================================================
# requirements.txt — ml-incident-response-playbook
# Last updated: 2026-05-22
# Python: >=3.11
# All packages pinned to security-reviewed 2024+ releases.
# Run `pip-audit --requirement requirements.txt --strict` before deploy.
# ============================================================

# ── FastAPI stack ────────────────────────────────────────
fastapi==0.111.0
uvicorn[standard]==0.29.0
httpx==0.27.0               # Test client for FastAPI

# ── Authentication & security ───────────────────────────
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
cryptography==42.0.8        # CVE-2023-49083 patched in 41.0.6+

# ── Data validation ─────────────────────────────────────
pydantic==2.7.1
pydantic-settings==2.2.1

# ── Structured logging ──────────────────────────────────
structlog==24.2.0

# ── Data science / ML ─────────────────────────────────
pandas==2.2.2
numpy==1.26.4
scipy==1.13.1
scikit-learn==1.5.0

# ── Airflow (pinned; install separately in scheduler environment) ─
apache-airflow==2.9.1       # CVE-2024-25141 patched in 2.8.2+
apache-airflow-providers-http==4.10.0

# ── Alerting ───────────────────────────────────────────
requests==2.32.2            # CVE-2023-32681 patched in 2.31.0+

# ── dbt (data transforms) ──────────────────────────────
dbt-core==1.8.3
dbt-postgres==1.8.1

# ── Configuration ───────────────────────────────────────
pyyaml==6.0.1               # CVE-2017-18342 patched in 5.4+
python-dotenv==1.0.1

# ── Documentation / reporting ────────────────────────────
markdown==3.6
jinja2==3.1.4               # CVE-2024-22195 patched in 3.1.3+

# ── Visualization ────────────────────────────────────────
matplotlib==3.10.9
plotly==5.22.0

# ── Development / testing ───────────────────────────────
pytest==8.2.2
pytest-cov==5.0.0
pytest-asyncio==0.23.7
black==24.4.2
flake8==7.1.0
mypy==1.10.0
boto3==1.34.101             # AWS SDK (Secrets Manager, S3)
