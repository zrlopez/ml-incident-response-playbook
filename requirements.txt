# ─── ML Incident Response API — Production Requirements ──────────────────
# Remediation 2026-05-23:
#   - python-jose removed: CVE-2024-33663 (algorithm confusion JWT forgery)
#   - PyJWT added: actively maintained, explicit algorithm enforcement
#   - slowapi added: rate limiting on /auth/token
#   - passlib version locked with security note
#   - All versions pinned; generate hash-pinned lockfile via:
#     pip-compile requirements.in --generate-hashes --output-file requirements.txt

# ── Core API framework ────────────────────────────────────────────────────
fastapi==0.111.0
uvicorn[standard]==0.29.0
python-multipart==0.0.9

# ── Authentication ────────────────────────────────────────────────────────
# REPLACED python-jose (CVE-2024-33663) with PyJWT
PyJWT[crypto]==2.9.0
passlib[bcrypt]==1.7.4
bcrypt==4.1.3

# ── Rate limiting ─────────────────────────────────────────────────────────
slowapi==0.1.9

# ── Data validation ───────────────────────────────────────────────────────
pydantic==2.7.1
pydantic-settings==2.3.0

# ── Observability ─────────────────────────────────────────────────────────
structlog==24.2.0
opentelemetry-sdk==1.24.0
opentelemetry-instrumentation-fastapi==0.45b0
opentelemetry-exporter-otlp==1.24.0
prometheus-fastapi-instrumentator==7.0.0

# ── HTTP client (alerts, webhooks) ───────────────────────────────────────
httpx==0.27.0
requests==2.32.2

# ── Data pipeline / ML ops ───────────────────────────────────────────────
apache-airflow==2.9.1
dbt-core==1.8.1
numpy==2.0.0
pandas==2.2.2
scikit-learn==1.5.0

# ── AWS SDK ───────────────────────────────────────────────────────────────
boto3==1.34.101

# ── Crypto ───────────────────────────────────────────────────────────────
cryptography==42.0.8

# ── Templating ───────────────────────────────────────────────────────────
jinja2==3.1.4

# ── Dev / testing (not installed in production image) ────────────────────
# These are separated here for documentation; use requirements-dev.txt.
# pytest==8.2.0
# pytest-asyncio==0.23.6
# pytest-cov==5.0.0
# httpx==0.27.0  (already above, usable in tests)
# bandit==1.7.8
# mypy==1.10.0
# pip-audit==2.7.3
