# ──────────────────────────────────────────────────────────────────────────────
# requirements-dev.txt — development and testing dependencies
# Install: pip install -r requirements-dev.txt
# Last reviewed: 2026-05-24
# ──────────────────────────────────────────────────────────────────────────────

# Production deps are a prerequisite
-r requirements.txt

# ── Test runner ──────────────────────────────────────────────────────────────
# pytest 9.x is incompatible with all pytest-asyncio 0.x releases (requires pytest<9).
# pytest-asyncio 1.0.0 adds pytest 9 support but is a major version bump;
# downgrading pytest to 8.3.x (latest stable 8.x) is the safer fix.
pytest==8.3.5
pytest-cov==6.1.0
pytest-asyncio==0.26.0
# anyio provides the async test runner for @pytest.mark.anyio in test_api.py.
# trio backend is used (faster for unit tests than asyncio in most pytest setups).
anyio[trio]==4.9.0

# ── HTTP test client (used with httpx.AsyncClient in test_api.py) ─────────────
httpx==0.28.1

# ── Type checking and linting ────────────────────────────────────────────────
mypy==1.15.0
ruff==0.11.10

# ── Security audit ────────────────────────────────────────────────────────────
bandit[sarif]==1.9.4
pip-audit==2.9.0

# ── Pre-commit hooks ──────────────────────────────────────────────────────────
pre-commit==4.6.0
hypothesis>=6.155.0
testcontainers>=4.8
docker>=7.0
