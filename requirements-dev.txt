# ──────────────────────────────────────────────────────────────────────────────
# requirements-dev.txt — development and testing dependencies
# Install: pip install -r requirements-dev.txt
# Last reviewed: 2026-05-24
# ──────────────────────────────────────────────────────────────────────────────

# Production deps are a prerequisite
-r requirements.txt

# ── Test runner ──────────────────────────────────────────────────────────────
pytest==9.0.3
pytest-cov==6.1.0
# pytest-asyncio 0.25.x supports pytest>=9; 0.24.x requires pytest<9.
pytest-asyncio==0.25.3
# anyio provides the async test runner for @pytest.mark.anyio in test_api.py.
# trio backend is used (faster for unit tests than asyncio in most pytest setups).
anyio[trio]==4.9.0

# ── HTTP test client (used with httpx.AsyncClient in test_api.py) ─────────────
httpx==0.28.1

# ── Type checking and linting ────────────────────────────────────────────────
mypy==1.15.0
ruff==0.11.10

# ── Security audit ────────────────────────────────────────────────────────────
bandit[sarif]==1.8.3
pip-audit==2.9.0

# ── Pre-commit hooks ──────────────────────────────────────────────────────────
pre-commit==4.2.0
