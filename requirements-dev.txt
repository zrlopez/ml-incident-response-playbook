# requirements-dev.txt — development and CI tooling
# NOT installed in production images (see infrastructure/Dockerfile.hardened)
# Install with: pip install -r requirements-dev.txt
#
# These tools are invoked by CI jobs in ci_cd/secure-ci.yml.

-r requirements.txt

# ── Testing ──────────────────────────────────────────────────────────────────────
pytest==9.0.3
pytest-asyncio==0.24.0
pytest-cov==5.0.0
httpx==0.27.2
aiosqlite==0.20.0

# ── SAST and security ────────────────────────────────────────────────────────────
bandit==1.7.10
pip-audit==2.7.3

# ── Type checking ───────────────────────────────────────────────────────────────
mypy==1.11.2

# ── Linting and formatting ───────────────────────────────────────────────────────
black==24.10.0
flake8==7.1.1
