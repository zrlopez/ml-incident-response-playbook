# ──────────────────────────────────────────────────────────────────────────────
# requirements-airflow.txt — Airflow worker / scheduler image ONLY
#
# CRIT-C REMEDIATION: Airflow is installed ONLY in the dedicated Airflow image,
# not in the API container. The API container has no orchestration dependency.
#
# Usage:
#   docker build -f Dockerfile.airflow -t ml-airflow:latest .
#   pip install -r requirements-airflow.txt  (inside Airflow image build)
# ──────────────────────────────────────────────────────────────────────────────

apache-airflow==3.2.1

# Airflow providers used by DAGs in this project
apache-airflow-providers-postgres>=6.0.0,<7.0.0
apache-airflow-providers-amazon>=9.0.0,<10.0.0
apache-airflow-providers-slack>=9.0.0,<10.0.0

# Shared data layer utilities (no web framework deps)
numpy==2.2.6
pandas==2.3.3
scikit-learn==1.6.1
tenacity==9.1.2
structlog==25.4.0
